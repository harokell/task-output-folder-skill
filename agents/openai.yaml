interface:
  display_name: "Task Output Folder"
  short_description: "Store generated files under task-outputs"
  default_prompt: "Use $task-output-folder whenever this task creates files, scratch artifacts, or staging copies without an explicit destination."
